<cfcomponent extends="Application">
<!--- This is an empty cfc that is used by the /amin/ApplicationProxyReference.cfc in order to extend the Application.cfc in the admin folder to the base Application.cfc in the root directory. This is only extended by the ApplicationProxyReference.cfc when extending to an Application.cfc in the root folder. --->
</cfcomponent>
